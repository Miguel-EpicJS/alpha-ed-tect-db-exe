/* FUTURE IMPLEMENTATION */

const commonRoutes = require('express').Router();

commonRoutes.post("/menu-routes", (req, res) => {}); // Route to get all routes for create a front-end menu

commonRoutes.post("/run-tests", (req, res) => {}); // Route to give the command to run back-end tests

module.exports = commonRoutes;